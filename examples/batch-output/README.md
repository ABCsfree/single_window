# 批量输出示例

更新后的生成器会为每个箱号输出一组文件：

- `BOX00123_ELBP004.xml`：信息补录主报文，包含表头、箱号项及附件元数据。
- `BOX00123_A1_ELBP005.xml` 至 `BOX00123_A4_ELBP005.xml`：每个图片各一份随附单据报文。
- 启用 P0 时另有 `BOX00123_P0_ELBP005.xml`。

`ELBP004` 与对应 `ELBP005` 使用相同的 64 位 `EdocID`。实际 XML 可通过应用生成，避免示例中的签名、时间和流水号被误当成固定值。
