# 批量输出示例

更新后的生成器会为整批输出：

- `ELBP004_<流水号>_<时间戳>.xml`：整批唯一的信息补录主报文，包含全部箱号项及附件元数据。
- `<箱号>_A1_<时间戳>.xml` 至 `<箱号>_A4_<时间戳>.xml`：每个箱号固定四份随附单据报文。
- 启用 P0 时另有整批唯一的 `0_P0_<时间戳>.xml`。

`ELBP004` 与对应 `ELBP005` 使用相同的 28 位 `EdocID`。实际 XML 可通过应用生成，避免示例中的签名、时间和流水号被误当成固定值。
